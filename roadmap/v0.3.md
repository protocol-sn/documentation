# Goals for version 0.3

1. User plugin goals
   1. Add users to groups
   2. Remove users from groups
   3. Default user groups on approval
   4. User verification
   5. Role-based link availability in UI
2. Node manager goals
   1. Smarter user registration
      1. Currently plugins will create a duplicate db record when they re-register after going offline. Don't do this
3. Blog plugin goals
   1. Submit plaintext blog
      1. Has blog text, title, tags, and timestamp
   2. Display in UI